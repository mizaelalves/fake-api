{
  "scripts": {
    "dev": "json-server --watch db.json --port 3333"
  },
  "dev-dependencies": {
    "init": "^0.1.2",
    "json-server": "^0.16.3"
  },
  "devDependencies": {
    "json-server": "^0.16.3"
  }
}
